export type MobileEvent = {
  id: string;
  title: string;
  date: string;
  venue: string;
  address: string;
  price: string;
  tag: string;
  featured: boolean;
  age: string;
  imageUrl?: string;
  bannerImageUrl?: string;
};
